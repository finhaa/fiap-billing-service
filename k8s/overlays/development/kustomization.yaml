apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ftc-app

bases:
  - ../../base

namePrefix: dev-

commonLabels:
  environment: development

replicas:
  - name: billing-service
    count: 1

configMapGenerator:
  - name: billing-service-config
    behavior: merge
    literals:
      - DYNAMODB_BUDGETS_TABLE=fiap-budgets-dev
      - DYNAMODB_PAYMENTS_TABLE=fiap-payments-dev
      - EVENT_BUS_NAME=fiap-tech-challenge-event-bus

images:
  - name: billing-service
    newName: billing-service
    newTag: dev-latest

patches:
  - target:
      kind: ServiceAccount
      name: billing-service
    patch: |-
      - op: replace
        path: /metadata/annotations/eks.amazonaws.com~1role-arn
        value: arn:aws:iam::305032652600:role/fiap-billing-service-dev
